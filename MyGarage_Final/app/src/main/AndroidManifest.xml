<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mygarage_final">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OthersListActivity" />
        <activity android:name=".ElectricListActivity" />
        <activity android:name=".DealerListActivity" />
        <activity android:name=".SparepListActivity" />
        <activity android:name=".MetalListActivity" />
        <activity android:name=".PaintListActivity" />
        <activity android:name=".MechanicListActivity" />
        <activity android:name=".VentListActivity" />
        <activity android:name=".TiresListActivity" />
        <activity android:name=".Categories" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>